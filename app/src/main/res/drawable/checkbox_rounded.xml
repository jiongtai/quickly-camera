<?xml version="1.0" encoding="utf-8"?>
<!-- checkbox_rounded.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <shape android:shape="oval">
            <solid android:color="@color/black"/> <!-- 选中时的颜色 -->
            <stroke android:width="2dp" android:color="@color/black"/> <!-- 边框颜色和宽度 -->
            <size android:width="40dp" android:height="40dp"/> <!-- 圆形大小 -->
        </shape>
    </item>
    <item android:state_checked="false">
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent"/> <!-- 未选中时透明 -->
            <stroke android:width="2dp" android:color="@color/black"/> <!-- 边框颜色和宽度 -->
            <size android:width="20dp" android:height="20dp"/> <!-- 圆形大小 -->
        </shape>
    </item>
</selector>
